export const CURRENCIES = ["USD", "HKD"] as const;

export const DEFAULT_CURRENCY = "USD";

// Static MVP assumption. Replace with a live FX feed when backend services are added.
export const HKD_PER_USD = 7.8;

