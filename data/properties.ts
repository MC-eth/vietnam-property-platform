import { getMockProperties } from "@/lib/repositories/mockRepository";

// JSON-backed mock property collection.
// Replace the repository implementation with MongoDB queries when the backend is enabled.
export const properties = getMockProperties();

